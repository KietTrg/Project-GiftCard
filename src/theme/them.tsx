import { ThemeConfig } from "antd";

export const them: ThemeConfig = {
    components:{
        Typography:{
            colorText: '#fff',
            titleMarginBottom: 0,
            titleMarginTop: 0
        }
        
    }
}